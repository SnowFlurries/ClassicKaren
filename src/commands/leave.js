const Discord = require('discord.js')

module.exports = {
  name: 'leave',
  description: 'Gtfo of the channel KAREN',
  execute(msg, args, state) {
    msg.reply('LMAO nice try, you\'re sitting through this one bucko')
  }
}